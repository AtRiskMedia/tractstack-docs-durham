---
import { Tabs, TabItem } from '@astrojs/starlight/components';
const id = 'page';
const path = Astro.url.pathname;
---
<astro-tab data-id={id}>
    <Tabs>
        <TabItem label=1></TabItem>
        <TabItem label="2"></TabItem>
    </Tabs>
</astro-tab>

<script>
    import { checkMap } from './tutorialStore.ts';
    console.log('example', checkMap)

    class Tab extends HTMLElement{
        constructor(){
            super();

            // Variables
            const id = this.dataset.id;
            console.log('id',id)
        }
    }
    customElements.define('astro-tab', Tab)
</script>